[build]
  command = "yarn build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Cache the .next directory between builds
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
