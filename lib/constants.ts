/**
 * Default quiz pass threshold (percentage) used when a quiz/challenge does not
 * specify its own `passingScore`. Mirrors the backend mastery gate of 80%.
 */
export const DEFAULT_QUIZ_PASSING_SCORE = 80;
